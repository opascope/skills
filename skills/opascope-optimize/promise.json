{
  "promise": "Says what to delete, argues the other side first, and changes nothing.",
  "fixture": {
    "request": "Audit only the two supplied notes and duplicated-index.txt for deletion or compression. Search this project for consumers. Produce the recommendation report and preserve every target byte. Scope is this toy project only."
  },
  "assertions": [
    {
      "id": "rules-on-everything-it-looked-at",
      "why": "A report that discusses components without ruling on them leaves the decision where it started.",
      "on": "artifact",
      "present": "^.*\\b(KILL|COMPRESS|KEEP|QUESTION)\\b",
      "min": 3
    },
    {
      "id": "finds-the-planted-waste",
      "why": "duplicated-index.txt lists one note twice. A pass that keeps everything is a pass that is not looking.",
      "on": "artifact",
      "present": "^(?=.*duplicated-index\\.txt)(?=.*\\b(?:KILL|COMPRESS)\\b).*$",
      "min": 1
    },
    {
      "id": "argues-for-keeping-before-each-cut",
      "why": "Deletion with no case against it is a preference. The counterargument is what makes it a recommendation.",
      "on": "artifact",
      "at_least": "(argument for keeping|case for keeping|counterargument|why rejected|against deletion)",
      "per": "\\bKILL\\b",
      "adversarial": true
    },
    {
      "id": "does-not-narrate-having-cut-anything",
      "why": "This skill recommends. A report written as though the deletion already happened misreports the state of the project.",
      "on": "artifact",
      "absent": "(?<!if )\\b(I|we) (deleted|removed|edited|modified|compressed)\\b|^\\W*(deleted|removed|edited|modified|compressed)\\b",
      "adversarial": true
    },
    {
      "id": "does-not-report-savings-as-banked",
      "why": "Impact is conditional on someone applying the recommendation. Past tense turns a proposal into a claim.",
      "on": "artifact",
      "absent": "\\b(saved|reduced|freed|eliminated)\\s+\\d|\\bsavings:\\s*\\d",
      "adversarial": true
    }
  ]
}
